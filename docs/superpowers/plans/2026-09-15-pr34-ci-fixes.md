# PR 34 CI Reliability Fix Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans to implement this approved plan task-by-task.

**Goal:** Fix recording-eval truncation and Codex plan reformatting without weakening either requirement.

**Architecture:** Bound recording responses and give those calls explicit token headroom. Strengthen Codex's existing verbatim forwarding instructions, keep the preservation assertion unchanged, regenerate artifacts, and validate with CI-equivalent settings.

**Tech stack:** Python/pytest/deepeval, Anthropic evaluation client, Codex Responses API, Markdown instructions and generated skills.

**Spec and approval:** Approved in this conversation after the investigation below. The original uncommitted plan was removed with its temporary worktree; this file restores its requirements and execution record. Implementation uses the existing PR branch in `/private/tmp/ship-pr34-ci-fix`.

## Failure evidence

PR #34 head `08f8b93c1e5b47837306e6631336b387f1ebfbf9`, Actions run `34962032340`:

- `evals-pr`: 157 passed; `test_recording_execution[multi-user]` hit `stop_reason=max_tokens` at 8,192 output tokens, including 5,231 thinking tokens. Its visible answer stopped mid-report.
- `evals-codex`: first repetition failed the exact plan-preservation assertion after Markdown reformatting; the next two repetitions passed all 25 tests.

## Constraints

- Preserve the recording rubric's 0.9 threshold, all six recording cases, per-case/per-role boundaries, no-tools honesty, and rejection of truncated/empty responses.
- Preserve the exact report assertion; do not normalize away Markdown differences or substitute semantic similarity.
- Keep global model/token defaults unchanged. Only recording evals receive a 16,384-token ceiling.
- No retries-until-green, new QA functionality, translation changes, live dynamic startup, fixtures, or evidence uploads.
- Preserve unrelated main-checkout work. Update the existing PR, not a new one.
- Retain diagnostics only for model, stop reason, token counts, and final user-visible output; do not copy private thinking or raw model response objects into reports or fixtures.

## Task 1: Focus and complete recording eval responses

- [x] Add a keyword-only `max_tokens` override to `call_model`, defaulting to the existing `MAX_TOKENS`, and forward it through QA `ask`.
- [x] Add fake-client coverage proving ordinary requests retain the default and explicit requests use 16,384 without changing messages, tools, or model.
- [x] Set `RECORDING_EVAL_MAX_TOKENS = 16_384` only for recording tests.
- [x] Request concise ordered timelines with setup-boundary summaries, tested actions/outcomes, capture/cleanup commands, failure handling, and evidence lines. Exclude fixture scripts, repeated plans, full PR bodies, and generic publication commands. Aim for 1,000 words without feeding the desired recording boundaries into the input.
- [x] Clarify that the recording rubric judges proposed commands and ordering, not unavailable live execution. Keep every behavioral criterion and threshold.
- [ ] Run all six recording cases and three independent repetitions of the multi-user case, requiring complete passing responses.

## Task 2: Copy planner reports without formatting changes

- [x] Strengthen canonical Codex GATE 1 instructions to copy wording, punctuation, filenames, command separators, list numbering, and existing Markdown unchanged; keep parent introduction/approval/table outside; strip only transport metadata; compare before sending; never implement before approval.
- [x] Keep `assert_report_preserved` unchanged. Add mutation tests rejecting path backticks, bold labels, and semicolon commands rewritten as a fenced block; retain exact-copy and existing blockquote acceptance.
- [x] Regenerate distributed Codex skills.
- [x] Run the unchanged plan-completion test three independent times using CI settings: Responses API, gpt-6-astra, medium reasoning. Preserve wait/approval checks and exact report validation.

## Task 3: Release and verify PR 34

- [x] Patch orchestrator 7.0.0 → 7.0.1 and package 1.15.0 → 1.15.1, including all manifests and expected versions. QA agent remains 5.0.0.
- [x] Document the fixes and regenerate final artifacts.
- [x] Run deterministic tests, generator drift checks, and whitespace checks.
- [ ] Run the full agent/orchestrator suite, plus three independent full Codex runs with CI settings. Preserve all run outcomes; use saved credentials only in evaluation subprocesses.
- [ ] Commit/push the verified fix to PR #34 and inspect its new Actions run. Do not report CI fixed until both required jobs pass on the updated head.

## Execution evidence

- Restored committed PR branch after temporary-worktree cleanup; main checkout's unrelated edits were preserved.
- RED: new token-override harness test failed with unexpected keyword `max_tokens`; default request still passed.
- GREEN: six focused harness unit tests passed after adding the override.
- Verbatim mutation tests: 28 passed; preservation assertion unchanged.
- Initial complete deterministic suite: 114 passed.
- Response diagnostics now assert a scalar stop reason so pytest does not expand the raw API response into truncation errors.

- All six focused recording evals passed (65.74s). Three independent focused plan-forwarding runs passed (16.83s, 16.30s, 18.35s).
- User explicitly requested pushing at this point. Full model-suite validation and three additional multi-user repetitions have not run locally; new CI results remain pending.
